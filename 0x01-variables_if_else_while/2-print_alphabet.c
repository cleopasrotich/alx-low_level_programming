#include <stdio.h>
#include <stdlib.h>

/**
 * main - To determine the alphabet in lower case
 *
 * Return: 0 on success
 */

int main(void)
{
char i;
putchar("\n");
for (i = 'a'; i <= 'z'; i++)
{
putchar("%c", i);
}
return (0);
}
